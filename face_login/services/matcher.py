"""Matcher: cosine-similarity nearest-neighbour search over the gallery.

Single responsibility: compare a probe embedding against stored embeddings and
apply the decision threshold (default 0.44). Implementation deferred to
Phase 7.
"""
