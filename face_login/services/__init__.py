"""Service layer: application use cases and orchestration logic.

Holds the register and login entry points plus their supporting logic —
coverage tracking (registration) and gallery matching (login). Orchestrates the
CV and database layers through their interfaces; contains no perception code.
"""
